services:
  postgres:
    image: postgres:15-alpine
    container_name: lims-dev-db
    environment:
      POSTGRES_USER: lims_dev
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: lims_dev
    ports:
      - "5433:5432"
    volumes:
      - lims_dev_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lims_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quimios-dev-app
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: "Host=postgres;Port=5432;Database=lims_dev;Username=lims_dev;Password=dev_password"
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

volumes:
  lims_dev_data:
